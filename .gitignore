__pycache__/
*.pyc
.venv/
.env
.env.local
# 运行期数据目录整体忽略：SQLite 主库及 WAL/SHM 伴生文件、gateway.log、
# 测试库都是运行期产物。此前只逐条忽略 *.sqlite3*，`data/gateway.log`
# 落在规则外——一次 `git add data/` 就会把日志提交进版本库。
# 目录本身无需入库：settings.DATA_DIR 启动时 mkdir(parents=True) 自动创建。
data/
# 个人工具配置（含明文 API Key），勿提交到版本库
config.json
# 助手工作区（会话记忆、草稿），勿提交到版本库
.workbuddy/
node_modules/
.next/
*.tsbuildinfo
out/
dist/
.DS_Store

# 本地工具运行缓存（勿入库）
.cache/

# 临时诊断/探针脚本与会话内一次性实验文件（勿入库）
_diag*.py
diag.py
diag[0-9]*.py
diag_req.py
_probe_*.py
_reset_*.py
_verify_*.py
_keepalive_db.sqlite3
.perf-test/

# Mimosa 安全扫描器运行时状态（会话/账本数据，勿入库）
.mimosa/
backend/.mimosa/
